## Example environment configuration for local development.
## Copy to `variables.env` and fill in your secrets. Never commit `variables.env`.
#
# cp variables.env.example variables.env
#

GPT_API_KEY=
SPRING_PROFILES_ACTIVE=local

# Email configuration for review notifications (HTTP API - no SMTP required!)
# Supported providers: brevo, sendgrid, mailgun
EMAIL_API_PROVIDER=brevo
EMAIL_API_KEY=
EMAIL_FROM_ADDRESS=
EMAIL_FROM_NAME=Bewerbung AI

# Review email recipient + toggle
REVIEW_EMAIL_RECIPIENT=
REVIEW_EMAIL_ENABLED=true

# Mailgun specific (only if EMAIL_API_PROVIDER=mailgun)
EMAIL_MAILGUN_DOMAIN=

